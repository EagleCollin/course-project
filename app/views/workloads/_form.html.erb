<%= form_with(model: workload) do |form| %>
  <% if workload.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(workload.errors.count, "error") %> prohibited this workload from being saved:</h2>

      <ul>
        <% workload.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :student_id, style: "display: block" %>
    <%= form.text_field :student_id %>
  </div>

  <div>
    <%= form.label :course_id, style: "display: block" %>
    <%= form.text_field :course_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
